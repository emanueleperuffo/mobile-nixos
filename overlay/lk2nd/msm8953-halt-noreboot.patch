--- a/platform/msm_shared/debug.c
+++ b/platform/msm_shared/debug.c
@@ -36,6 +36,8 @@
 #include <string.h>
 #include <arch/arm/dcc.h>
 #include <dev/fbcon.h>
+#include <reg.h>
+#include <platform/iomap.h>
 #include <dev/uart.h>
 #include <platform/timer.h>
 #include <platform.h>
@@ -189,6 +191,9 @@
 		reboot_device(PANIC_REBOOT_MODE);
 	}
 
+	/* Stop the APPS watchdog so it cannot bite and reset us
+	 * while we sit on the crash screen. */
+	writel(0, 0x0B017004);
 	dprintf(CRITICAL, "HALT: spinning forever...\n");
 	for (;;) ;
 }
